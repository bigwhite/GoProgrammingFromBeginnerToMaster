// 注意：这不是一个可以编译通过的源文件，仅用于gofmt演示
package main

import "fmt"

func main() {
	fmt.Println(s[3:len(s)])
	n, err := s.r.Read(s.buf[3:len(s.buf)])
	reverseLabels = append(reverseLabels, domain[3:len(domain)])
	reverseLabels = append(reverseLabels, domain[i+1:len(domain)])
}
