// +build ultimate

package main

func init() {
	featureList = append(featureList, featureAttr{
		name: "e",
		ver:  "ultimate",
	})
	featureList = append(featureList, featureAttr{
		name: "f",
		ver:  "ultimate",
	})
}
